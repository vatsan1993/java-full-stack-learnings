//  This is will contain examples for working with jdbc. not necessary for the project

package com.example.ims.ui;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

public class Demo {
    // public static void main(String[] args) {

    //     // using try with syntax to close the jdbc connection automatically
    //     try(Connection con = DriverManager.getConnection("jdbc:mysql://localhost:3306/imsDb", "root", "ssvnka302#")){
    //         // by default the auto commit is true, so we need to set it to false
    //         //  con.setAutoCommit(false); // this is used to disable auto commit in jdbc

    //         System.out.println("Connection Established");
    //     } catch (SQLException e) {
    //         // TODO Auto-generated catch block
    //         e.printStackTrace();
    //     }

    // }
}
